// Live driver-density heatmap — rendered as a MapLibre heatmap Layer inside
// LiveMap's <Source id="drivers">.  This file is kept as a no-op so any
// stale imports compile; the actual layer lives in LiveMap.tsx.
export default function HeatmapLayer() { return null }
